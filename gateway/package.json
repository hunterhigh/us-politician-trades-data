{
  "name": "unison-readonly-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Local prototype: authenticated immutable snapshot distribution",
  "scripts": {
    "test": "tsx --test tests/*.test.ts",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types --strict-vars false",
    "types:check": "wrangler types --strict-vars false --check",
    "build:check": "wrangler deploy --dry-run --outdir dist"
  },
  "keywords": [],
  "author": "",
  "type": "module",
  "engines": { "node": ">=24 <25" },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260918.1",
    "@types/node": "^26.6.1",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "wrangler": "^4.134.0"
  }
}
