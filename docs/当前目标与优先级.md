# 当前目标与优先级

生效日期：2026-09-21
状态：当前执行基线

2026-09-22 行情补查进展：正式生产运行 `35679392251` 使用 Alpaca 历史 SIP 接口补回 36 个未列在当前资产目录的历史代码，行情覆盖增至 1,229/1,984；755 个代码仍无可核实的 SIP 日线，不能称为全部有价格。候选与已发布版本均通过冻结前端浏览器验收。该轮还修正了定时任务读取环境级开关的时机；下一次真正的定时运行仍待观察。当前披露为 313 人、9,358 笔交易、1,840 条持仓，来源健康 4 条。最新生产提交与连续手动运行证据见[实现状态与下一步](实现状态与下一步.md)，不以本段历史提交作实时指针。

本文是项目当前工作的最高优先级说明。它覆盖《政客交易数据服务-总体设计 v0.3》《实现状态与下一步》及旧复核流程中与以下决定冲突的内容：取消逐条人工复核和具名人工批准作为生产晋级条件；停止把空快照或单一来源试点当作产品交付终点。

## 唯一当前目标

首发生产目标已于2026-09-21完成。当前目标是在不修改冻结前端业务功能和数据含义的前提下，稳定运行官方披露增量、Alpaca行情增量、五数组构建、冻结前端验收和原子发布，并用可审计覆盖信息持续说明尚未纳入的数据。

正式`main`已经是非演示、非空、含行情的生产快照。后续工作以生产稳定性、增量效率、Actions预算和可恢复性为先；House异常版式、Senate纸面报告和其他来源扩展进入明确积压，不再反向阻塞已经闭合的首发范围。

## 已确认的产品原则

1. 对方前端是验收目标。后端、数据布局和读取层适配前端，不要求前端为当前后台骨架删减功能。
2. 取消全量逐条人工复核、具名复核者和逐条人工批准门禁。已有人工复核命令和模板降为异常调查工具，不再阻止符合自动规则的标准记录发布。
3. 标准记录由确定性自动规则晋级。解析不确定、OCR置信不足、身份冲突、修订关系不明、金额语义无法表示或证据不闭合的记录进入隔离队列；隔离记录不阻塞其他合格记录。
4. 人工抽样审计可以用于评估解析质量，但不是每条记录的生产前置条件。来源用途和行情再分发许可仍属于项目级发布条件。
5. 不伪造缺失字段、价格、收益或覆盖范围。无法支持的数据必须明确标记缺失、隔离或不覆盖。
6. 完整候选快照先通过前端验收，再移动正式 `main`；生产发布保留固定 commit、内容哈希、发布标签和回退能力。

## 自动晋级规则

一条披露记录只有同时满足以下条件，才能自动进入候选事实：

- 来自约定的官方来源，并能定位到已归档原件、来源 URL 和 SHA-256；
- 解析器版本固定，文件版式属于已通过黄金样本验证的类型；
- 人物由官方稳定标识或无歧义规则确定，不能只依赖姓名猜测；
- 日期、交易类型、资产、金额语义和证券标识通过 schema 与业务校验；
- 修订、撤回和 amended 关系能够由确定性规则建立；无法确定时隔离；
- 去重、外键、证据引用和内容哈希检查通过；
- 没有静默丢行、截断或用默认值掩盖解析失败。

自动规则通过后，系统直接产生可进入候选快照的规范事实，不再要求 `reviewer`、`reviewed_at` 或逐行人工决定。任何规则失败都必须留下机器可读原因、原件引用和重试状态。

## 项目优先级

| 顺序 | 工作 | 完成结果 |
|---:|---|---|
| P0-1 | 稳定生产增量 | 工作日行情与来源增量按门禁运行；失败不移动`main`，成功产生固定提交和发布标签 |
| P0-2 | 控制运行成本 | 验证已实现的行情增量复用在连续生产运行中的节省量与输出一致性，持续观察Actions分钟、仓库增长和API调用量 |
| P0-3 | 补齐重点人物并保持消费契约 | 优先解决特朗普OGE直链原件的可靠日期和表格提取，合格事实进入现有快照；继续用冻结交接包的dashboard/search/person/ticker及浏览器链路验收 |
| P0-4 | 维护覆盖审计 | 所有披露代码必须精确落入已覆盖或无可核验Alpaca SIP行情并记录原因；缺失与未解析数量必须为0才能发布 |
| P1-1 | 收敛异常披露 | 按收益优先处理House异常版式、Senate纸面报告和隔离记录，不恢复逐条人工生产门禁 |
| P1-2 | 灾备与告警 | 增加GitHub之外的异地备份和失败告警，并定期验证历史标签回退 |

首发阶段原P0-1至P0-7已经完成：前端基线固定、三类官方披露与House持仓候选构建、Alpaca行情分类与回填、完整五数组、发布前后浏览器验收、`market`和`main`原子发布均已通过。

## 当前事实和差距

以下为截至2026-09-21的首发生产记录；最新计数与已知空缺见本文开头及下表：

- 正式`main`为`12c2c8e122f3f882fb17d56abfd505cf3d9157f2`，快照ID为`1cf8a82128cb66a4e9d2c073fc6a4d96aa2b7006095fc2588badf283ac98284d`；对应`published/<commit>`标签已创建。
- 行情`market`为`d5faa061ab7402e68b18061c0df75e42d5324ec4`；对应`published-market/<commit>`标签已创建，`main`清单固定引用该提交及24个内容寻址行情页。
- 五数组为313人、9,356笔交易、1,840条申报持仓、1,193条Alpaca SIP行情和4条来源健康记录；披露截止为2026-09-20，行情截止为2026-09-18。
- 1,984个披露代码全部被分类：1,193个具有行情，791个明确在Alpaca SIP范围外；缺失为0、未解析为0，另有338个代码由申报资产名称中的明确代码恢复。
- 完整候选与GitHub远端发布结果均通过冻结前端浏览器验收，dashboard/search/person/ticker固定提交回读通过。
- GitHub`production`已配置两项Alpaca Secret、市场分发授权、每批100个代码和完整发布开关；密钥不在任何Git ref或本机项目文件中。
- `universe_complete=false`继续保留：它说明当前输入并非美国全部政客和全部历史披露。OGE Form 201请求型报告不属于首发范围；House异常版式、Senate纸面报告和隔离记录保持可见积压。
- 首发完整发布曾每次重取较长行情历史；2026-09-22已合入增量复用代码，并经三次手动生产运行验证输出一致。真实工作日定时成本仍待观察。

### 当前已知数据空缺

| 空缺 | 当前范围与原因 | 处理原则 |
|---|---|---|
| OGE Form 201请求型报告 | 最近已确认的OGE目录中有4,775份；原件须经Form 201请求，尚未进入本项目官方原件和规范事实 | 保留目录计数与来源说明；不将未取得的报告记为无交易，不由Action匿名自动申请 |
| 无可核验行情的申报代码 | 2026-09-22生产快照中1,984个代码有1,229个取得Alpaca SIP日线，755个没有；包括共同基金、ADR/OTC、旧代码及其他非标准资产，具体类别仍以逐项证据为准 | 保留披露事实和无价状态，不填零、不借用其他证券价格；暂不接入新的免费订阅或更改生产行情来源，待有适用数据及公开再分发许可后再评估 |
| 白宫人员及特朗普交易 | 已归档OGE 278-T目录中，`White House Office`有132条：18份特朗普直链PDF、114份需Form 201请求；当前OGE生产候选没有白宫人员。特朗普18份提取结果均因签署/申报日期未能唯一确认而未晋级，多份还是扫描/OCR异常版式 | 作为前端重点人物的明确覆盖缺口处理；先修可靠日期与版式证据，再按原有自动资格规则发布。不得把目录日期、OGE接收日期或OCR猜测替代申报日期，也不得用无事实占位或演示数据冒充交易。Form 201仍按既定决定暂缓 |

Form 201与无可核验行情按既定决定暂缓；特朗普直链原件对应冻结前端的重点人物要求，列为当前优先修复的覆盖缺口。现有快照仍可运行，但不能因此声称特朗普交易数据已交付。

## 当前执行焦点

1. 观察已启用的完整发布定时任务，确认下一次无人工介入运行能稳定完成且失败时不移动生产指针。
2. 持续观察行情历史增量复用：核对实际工作日运行的请求页数、耗时、内容哈希与全量口径，保持拆股调整、完整分页和截止日规则不变。
3. 记录单次运行时长、Actions分钟、API请求页数、`main`/`market`增长和失败类型，达到预算阈值时降低频率或拆分更新。
4. 继续运行House、Senate和OGE官方增量；异常记录进入隔离，合格记录自动进入下一候选。对行情无价代码和Form 201保留覆盖记录，现阶段不开展新增供应商接入或请求型报告回填。
5. 把House异常版式、Senate纸面OCR和外部备份作为后续批次，不以它们阻塞已上线首发范围。

## 当前完成定义

首发完成定义已经满足：

- 对方冻结前端无需业务删减或临时假数据即可运行；
- 正式`main`包含非演示披露、持仓、行情和来源健康五数组；
- 行情许可门禁、Alpaca凭据、代码分类、缺失检查和市场分支固定引用均已闭合；
- 完整候选和GitHub远端发布结果均通过原前端浏览器验收；
- `main`、`market`及发布标签可固定读取并保留回退路径；
- 覆盖范围、截止时间、未纳入范围和来源健康在清单中可审计。

下一阶段以连续成功的定时增量、合理Actions消耗、失败告警和异地恢复能力作为完成标准。任何优化不得改变冻结前端契约、伪造价格或把未覆盖显示为零。
