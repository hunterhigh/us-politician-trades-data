# 实现状态与下一步

更新时间：2026-09-20。当前执行方向以[当前目标与优先级](当前目标与优先级.md)为准：取消逐条人工复核门禁，以完整披露、行情和原前端验收为唯一当前目标。自动资格判定、异常隔离和按需OCR已经进入生产工作流；旧复核模板只保留为异常调查工具。

## 本轮实际验收

| 验证 | 结果 |
|---|---|
| Python 生产器、公开读取、姓名解析、House 索引/PDF/身份/自动资格、来源候选与统一候选组装、三类旧表几何、OCR坐标与栅格勾选、零交易申报、精确小数金额、日期边界、跨页状态、期权条款、来源状态隔离、Senate目录/名册/身份/入口归档/电子PTR解析/多级修订链/历史补充证据/历史身份期限/候选晋级与双重采集门禁、OGE目录解析、增量检查点、CLI、发布失败保护、回退和备份恢复 | 162 项通过（以最新复跑为准） |
| Worker 鉴权、固定版本、demo 隔离、大小/时间限制及异常上游模拟 | 27 项通过 |
| 对方原包回归测试 | 29 项通过；不是新版所称 57 项测试 |
| Worker 类型、生成类型一致性、Wrangler 本地 dry-run | 均通过；未部署 |
| 实际原包处理器与渲染器链路 | 已从公开生产 `main` 固定到 commit `34b63c67b352951042a33ad41b8ab3e9e6a228e7`，读取并校验空生产快照；House候选以93人、3225笔真实交易通过生产构建器、原包处理器和原渲染器，Senate来源候选以23人、1435笔真实交易通过同一链路；共同完整日截止合并得到116人、4660笔交易并通过同一链路；合成输入另覆盖2人、2笔交易、1条持仓 |
| 工程基础核验 | ready-with-followups；Python 版本、版本记录及开工说明等共享文件相对脚手架的预期修改保留审阅提示 |

本机演示入口为 `../.local/demo/dashboard.html`，可用根目录的 `python backend/scripts/demo.py` 重建。测试未包含浏览器交互自动化、全量真实来源采集或新消费端代码。公开代码仓库及生产控制面已经上线；远端 CI、发布、回退、来源归档和复核队列均已有成功运行记录。

## 本轮交付

1. 输入规范校验、显式合成标识、金额范围、日期、外键、重复 ID、字段白名单；真实数据入口关闭。
2. 规范 JSON 字节、SHA-256 内容地址、people/tickers 哈希桶、看板包和 manifest；超出索引或分片预算直接失败，不截断记录。
3. `source_health` 仅在 manifest，健康变化不重写业务分片；业务截止时间改变会改变 snapshot_id，避免相对时间统计缓存过期却不刷新。
4. 独立裸 Git 数据仓库，比较预期旧提交后原子更新 demo 分支与 `published/<SHA>` 标签；失败保留前版、保留旧对象及标签、支持指定版本读取与回退。
5. 本项目本地读取工具重建看板、人物与标的数据；原包处理器和渲染器固定哈希，实际参与验证与页面生成。
6. 只读代理隔离实现，生产 main-only，固定仓库、鉴权、冻结提交、发布标签校验、路径白名单、大小与超时限制；离线模拟上游测试。
7. 离线 CI 配置、合成演示、Git bundle 备份恢复测试与运行说明。
8. 公开 GitHub 首发读取器：先把 `main` 解析成固定 commit，再按该 commit 读取所有文件；支持 dashboard/search/person/ticker，校验内容哈希、demo 边界、依赖和人物引用。人物入口可把唯一精确匹配的全名或短名解析为同一commit内的稳定ID；无匹配或重名明确失败。
9. 生产工作树物化与手动发布 workflow：生产输入门禁、manifest 最后写、旧内容保留、远端基线复核、`main` 与 `published/<SHA>` 原子推送。
10. House Clerk 单源发现第一段：读取官方年度 ZIP 索引，校验文件布局与行字段，按文件类型生成官方 PDF 地址，并按 SHA-256 不可变归档原始 ZIP 与首次获取元数据。真实 2026 索引验证为 1,653 条，其中 393 条 PTR；空日期保留为未知。所有记录仍标记为 `official_raw_unparsed`。
11. House PTR 受控解析：只有已归档索引确认的 PTR 才能下载；校验重定向、大小、PDF 头尾和内容哈希。电子表解析现已覆盖股票、期权、无 ticker、金额换行、精确金额、开放金额、跨页末行和 amended 申报；每行保留页码与坐标证据。
12. House 人物身份：解析 Clerk 当前议员 XML，跳过官方空缺席位，用选区和姓名共同匹配 BioGuide 稳定 ID；唯一精确匹配直接标记 `matched_automatically`，冲突保持 `unresolved`。
13. 自动资格与隔离：标准行直接生成 `official_matched` 候选交易；身份不确定、修订关系不明、开放金额无法按前端上下界表达或OCR置信度不足的行进入带原因的隔离记录。旧具名复核命令保留，但不再是目标发布路径。
14. 可恢复增量检查点：真实 2026 规划当前得到395份PTR，395份已全部归档、待处理/下载失败/索引异常均为0；失败退避、索引变更和证据异常检查保持不变。定时流水线继续发现后续新增申报。
15. 生产资格队列：395份原件全部有互斥状态，364份抽取成功、31份旧式或异常版式失败、待解析和状态冲突均为0；339份文件产生3225条候选交易，39份文件中的392条记录被隔离，另有4份可靠识别为无交易申报。解析重试失败会删除旧抽取、身份和资格结果，避免旧数据残留在候选。动态计数以 `review/status/house_clerk.json` 为准，`summary.json` 暂作兼容别名。
16. House前端候选：自动资格产物、稳定人物和来源健康已组装到 `review/candidates/house-current.json`；当前包含93人和3225笔交易，按冻结前端实际窗口语义近30天80笔，范围为2023-10-31至2026-09-08；19笔完整披露条款的期权已保留类型、行权价和到期日，3笔条款不完整的期权进入隔离。候选不含行情或持仓，尚不替换正式 `main`。
17. 生产控制面：`production` environment 只允许 `code` 触发；五个生产分支和 `published/*` 标签禁止删除及强推；正式回退采用新提交恢复旧树并已完成无变更演练。
18. 多来源第一段：House运行状态新增 `status/house_clerk.json`，旧文件名保留为兼容别名；来源采集与资格工作流改用跨来源统一写入锁。OGE目录解析器已严格校验分页、字段和官方URL，把278-T区分为官方直接PDF与需Form 201请求，且不会把目录加入日期冒充申报日期或猜测申报编号。
19. 统一披露候选：各来源候选进入 `review/candidates/sources/` 后按确定性顺序合并；身份冲突、事实ID重复、外键缺失、来源归属和截止时间不一致均失败关闭。House工作流同时保留 `house-current.json` 兼容入口，并生成 `disclosure-current.json` 与统一状态。
20. Senate禁用态地基：eFD搜索结果按严格五列与完整分页校验，电子/纸面PTR分别标识，门户展示日期不冒充 `filed_at`；官方参议员XML生成 `senate:<bioguide>` 稳定身份，姓名和州必须唯一匹配。独立生产workflow已成功归档100人官方名册、原始XML、规范名单与元数据，并更新运行状态；它不访问报告门户或接受声明。
21. House失败队列收敛第一批：身份在原姓名加选区精确规则之外，允许同一官方选区内唯一姓氏，以及同州唯一完整姓名的改区恢复；任何同名、同姓、目标选区或持久名册冲突仍保持未解析。明确写有无交易声明且不存在表格行证据的申报可记录为成功零行，不能用空白或解析不到交易来冒充零交易。生产流程会刷新全部身份，并按新解析器版本重试旧失败文件。
22. Senate生产链路：`senate-efd.yml`、严格环境开关、受门禁保护的会话与完整分页归档已投入生产。2026-01-01至今130份PTR目录和入口响应已全部内容寻址归档；121份电子PTR经真实HTML版式、标题日期、修订编号、页面交易总数、表格行数和原件哈希闭合校验后生成1,646条抽取交易，0个入口下载失败、0个当前解析器失败。9份纸面PTR确认返回HTML扫描查看器，当前仅归档入口并标记需要逐页资源收集，未冒充完整PDF或完整证据。身份结果为104份精确、15份官方目录别名、11份未解析；未解析记录继续隔离。原始响应进入 `evidence`，身份和解析结果进入 `review`，运行状态进入 `state`。
23. Senate自动晋级与修订链：来源状态、目录、名册、身份、解析器版本、内容寻址路径和原件SHA必须一致；只允许`exact/alias + matched_automatically`身份与资格状态一致的标准行晋级。修订要求唯一前序、完整行号、至少三个事实锚点、最多一行扩展修正及官方提交时间递增；多级链淘汰全部祖先，同序号未链接报告继续隔离。受限历史工作流从426条目录中固定16份官方候选原件，为12个缺前件组全部建立唯一正文关系；12份选中前件的188行只参与resolver，不进入主交易输入。当前GitHub生产结果为23人、1,435笔候选，170笔隔离，41笔主目录旧版superseded，16条链；审计保持`1,435 + 170 + 41 = 1,646`主输入守恒。Congress.gov历史身份补充、届次与任期校验已实现，尚待配置免费`CONGRESS_GOV_API_KEY`后生产验证。
24. 多来源共同截止：House与Senate异步运行时显式取最早运行日之前的最后完整UTC日，按`filed_at`过滤更晚事实并裁剪无引用人物；来源原生水位相差超过两天、事实晚于本源水位或非空行情缺少独立策略时失败关闭。当前生产来源候选合并为116人、4,660笔交易，本批过滤0笔；机器审计记录每源输入SHA、原生水位和过滤前后数量。Senate历史补充运行[`35502283617`](https://github.com/hunterhigh/us-politician-trades-data/actions/runs/35502283617)与常规复跑[`35502389024`](https://github.com/hunterhigh/us-politician-trades-data/actions/runs/35502389024)均成功，远端`review` commit为`0e39fa5f4f3571d12d767d6e9d8aeca484c4562e`；常规目录仍为130份、待归档0，证明历史原件没有污染日常增量计数。

## 仍未交付的生产能力

| 项目 | 原因与下一步 |
|---|---|
| House 多版式与修订链 | 电子 PTR 已覆盖跨页、期权、无 ticker、换行金额和 amended 申报；旧式纸表已覆盖三类列布局、封面状态复用、示例行排除和栅格勾选检测。修订决策门已实现；仍需收敛31份失败文件及392条隔离记录，并在正式数据中验证旧记录关联 |
| 自动事实晋级 | 自动资格校验、异常隔离和House候选聚合已进入生产工作流；每次资格运行都会重建候选并用原渲染器验收，下一步持续扩大覆盖并接入其他来源 |
| OGE 真实采集 | 官方目录离线解析与直接/请求分类已实现；生产分页同步、内容寻址目录归档、直接PDF下载、278-T解析、身份和资格尚未实现。请求型报告需要Form 201与受控邮件入站，不能由Action匿名代替 |
| Senate 真实采集 | 100人名册、130份目录和入口已归档，121份电子PTR已解析出1,646条交易；12个历史前件组已由16份官方原件自动闭合，23人、1,435笔进入候选，170笔隔离，41笔主目录旧版superseded，并已与House按共同完整日水位合并。下一步给production environment配置免费`CONGRESS_GOV_API_KEY`并验证11份未解析身份；为9份纸面查看器归档52页闭合证据 |
| House 增量回填 | 当前官方索引395份已全部归档且无下载失败；定时任务继续处理新增申报。31份解析失败和392条隔离记录保持可见，低置信度旧表不会自动进入候选事实 |
| 对方正式消费端 | 本项目已提供 `client/snapshot_repo.py` 与公开 GitHub fetch 包装，但对方声称完成的原版文件及57项测试仍未交付，需互相联调 |
| 行情分支 | 默认关闭；没有市场价格分片和跨分支发布，不能声称原前端完整价格功能已交付 |
| 覆盖范围展示 | 输入全部不等于全体政客；manifest 明确 universe_complete=false，尚需新版消费端展示并约束跳转范围 |
| 云资源与真实 HTTP | 公开 GitHub 仓库即当前生产 HTTP 源；未创建 R2，也未部署可选 Worker。匿名 GitHub API 有较低限额，自动消费者宜提供 `GITHUB_TOKEN` |
| 远端发布 | 公开生产仓库已部署；`main` 发布正式快照并用 `published/<SHA>` 固定，`state` 持久化检查点，`evidence` 保存原件，`review` 保存抽取、资格与隔离产物。保护规则和历史保留式回退均已配置并演练；当前快照仍为 `bootstrap_empty` |
| 自动运维 | House 每 6 小时刷新索引、向 `evidence` 归档有界批次 PDF，并把检查点写入 `state`；Senate名单每日更新，eFD每6小时刷新目录、归档最多25个未归档入口，并离线重放当前解析器；当前130份入口已追平。事实发布保持人工触发。尚未配置外部告警和 GitHub 之外的异地备份，需继续观察 Actions 月耗时与仓库增长 |

## 必须保留的协作边界

- 不修改 `review-input` 的前端基线，不把旧处理器测试当作新版消费端测试。
- 正式代理拒绝合成快照；本地 demo 不接入正式 main。
- `published/<SHA>` 用于证明“技术发布过”，不表示数据授权已成立；官方 raw 只进入 `evidence`，不得混入 `main`，密钥不得写入任何 Git ref。
- 目前按全部输入建 board，没有任意大规模截断/分页；触发大小阈值时应先调整契约和窗口，再恢复发布。
- snapshot_id 包含业务截止时间和展示配置。健康检查时间单独变化不影响它；generated_at 单独变化不创建无意义提交。

## 当前执行顺序

按[当前目标与优先级](当前目标与优先级.md)执行 P0-1 至 P0-7：固定实际前端验收基线；完成 House、Senate、OGE 披露回填；补齐 OCR、特殊金额、跨页和自动晋级；接入有合适生产与再分发权限的行情；构建完整候选快照；通过原前端全部模式和测试；最后原子发布非空正式快照。旧 D 阶段及逐条人工复核不再作为当前交付门槛。
