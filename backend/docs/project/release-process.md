# 版本与发布流程

1. 在短期分支完成一项独立变更。
2. 更新测试和 `CHANGELOG.md`。
3. 本地验证通过后进入 Pull Request 和自动检查。
4. 需要生产环境时，先验证完全相同的 commit/构建。
5. `code`变更经PR与自动检查合入；正式数据由受控工作流按已配置的定时或手动触发发布，执行固定提交只读回读与冻结前端浏览器复验，并记录版本与运行标识。定时发布修复后的连续无人介入成功仍待实测确认。
6. 失败时先判断代码回退还是数据修复，绝不把两者混为一体。
